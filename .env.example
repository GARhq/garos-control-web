# ⚠️ SECURITY (AURA-20260813-003): GEMINI_API_KEY NÃO é mais injetada
# no bundle do cliente. Se você precisa chamar Gemini, faça via backend
# Rust (garos-control-api) com auth, nunca direto do browser.
# Esta linha fica aqui apenas como lembrete/documentação.

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"
